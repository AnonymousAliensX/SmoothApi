name: smooth_api
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.18.6 <3.0.0'

dependencies:
  hive: ^2.2.3
  crypto: ^3.0.2
  http: ^0.13.5
  http_parser: ^4.0.2

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
